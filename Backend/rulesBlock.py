from Backend import CellArray

def UniqueCellColor(CellArray, searchedCellValue, wantedCellValue):
    for value in CellArray:
        if value == searchedCellValue:
            value = wantedCellValue

def OneNeighbourCellColor(CellArray, searchedCellValue, wantedCellValue):
    for value in CellArray:
        if ()

